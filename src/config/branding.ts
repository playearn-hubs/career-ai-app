import manifest from "../../assets/branding.manifest.json";

export const branding = manifest;

/** In-app logo — 192px variant from the favicon set */
export const appLogo = require("../../assets/favicon/web-app-manifest-192x192.png");
